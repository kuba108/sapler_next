/** Client-safe menu display constants (no DB / server-only deps). */
export const MENU_NAMES: Record<string, string> = {
  main_menu: 'Hlavní menu',
  product_categories_menu: 'Kategorie produktů',
  footer_menu: 'Menu v patičce',
};

export const MENU_LAYOUTS: Record<string, string> = {
  default: 'Defaultní',
};
